name: Deploy

on:
  push:
    branches:
      - source

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Deploy
        uses: naps62/zola-deploy-action@3eb7055
        env:
          PAGES_BRANCH: master
          TOKEN: ${{ secrets.DEPLOY_TOKEN }}
